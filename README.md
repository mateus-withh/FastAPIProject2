# FastAPI User API

Uma API simples construída com FastAPI que consome dados de uma API externa.

## Funcionalidades

- Listar todos os usuários
- Buscar usuário por ID
- Criar novo usuário

## Execução

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload